import styledComponents, {
  ReactNativeStyledInterface,
} from 'styled-components/native';

import { ThemeInterface } from './theme';

export default styledComponents as ReactNativeStyledInterface<ThemeInterface>;
